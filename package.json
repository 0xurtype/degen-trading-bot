{
  "name": "gmgn-scanner",
  "version": "1.0.0",
  "description": "Token scanner & wallet tracker — prototype",
  "scripts": {
    "dev": "npx serve public -l 3000 --cors"
  },
  "dependencies": {},
  "devDependencies": {}
}
